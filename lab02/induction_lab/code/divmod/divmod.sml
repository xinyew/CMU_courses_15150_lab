(* divmod : int * int -> int * int
 * REQUIRES: n >= 0, d > 0
 * ENSURES: divmod (n, d) ==> (q, r) with q * d + r = n, r < d, q >= 0, r >= 0.
 *)
fun divmod (_ : int * int) : int * int = (* Implementation goes here. *)
  raise Fail "unimplemented"  (* DELETE THIS LINE *)
